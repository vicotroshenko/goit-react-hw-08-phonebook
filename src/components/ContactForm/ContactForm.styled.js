import { styled } from '@mui/system';
import { Box } from '@mui/material';

export const StyledBoxFrom = styled(Box)`
	display: flex;
	flex-direction: column;
	row-gap: 20px;
	padding: 10px;
	border-radius: 14px;
	background-color: rgba(255, 255, 255, 0.5);
`